package com.tian.repository;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

//@Repository
//public interface NavigationRepository extends JpaRepository<Navigation, Integer> {
//public interface NavigationRepository extends JpaRepository< Object,Integer> {
    //@Query("select t from Navigation t")
    //Page<Navigation> pageNavigation(Pageable pageable);
    //Page pageNavigation(Pageable pageable);
//}
